import { SpatialIndexing } from '../src/SpatialIndexing'

/**
 * Dummy test
 */
describe('Dummy test', () => {
  it('works if true is truthy', () => {
    expect(true).toBeTruthy()
  })

  it('DummyClass is instantiable', () => {
    expect(new SpatialIndexing()).toBeInstanceOf(SpatialIndexing)
  })
})
